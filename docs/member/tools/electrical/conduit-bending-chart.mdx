---
id: conduit-bending-chart
title: Conduit Bending Chart
---
import useBaseUrl from '@docusaurus/useBaseUrl'
import {
  mdiDownload
} from '@mdi/js'
import Icon from '@mdi/react'

CertGround™ offers members a Conduit Bending Chart that can be viewed or downloaded for your offline use or if you want to print off the charts.

<img src={useBaseUrl('img/conduit-bending-chart.png')} alt="conduit bending chart page" />

:::note
You can download the charts with the download icon <Icon size={1} path={mdiDownload} /> near the top right of the charts.
:::
